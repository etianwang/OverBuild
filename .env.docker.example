# Docker Compose overrides (optional)
# Use when local PostgreSQL already occupies port 5432:
#   POSTGRES_PORT=5433 docker compose up -d
# Then set:
#   DATABASE_URL=postgresql://overbuild:overbuild@localhost:5433/overbuild?schema=public&client_encoding=UTF8
